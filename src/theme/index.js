import { brand, colors } from './colors';
import typography from './typography';
import { spacing, borderRadius, shadows } from './spacing';

export const theme = {
  brand,
  colors,
  typography,
  spacing,
  borderRadius,
  shadows,
};

export { brand, colors, typography, spacing, borderRadius, shadows };

export default theme;
